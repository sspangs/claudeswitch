#!/usr/bin/env bash
# Fake `uname` for tests: lets the suite force claudeswitch's platform
# detection to Darwin or Linux regardless of the host OS.
if [ -n "${FAKE_UNAME:-}" ] && [ "${1:-}" = "-s" ]; then
  printf '%s\n' "$FAKE_UNAME"
  exit 0
fi
exec /usr/bin/uname "$@"
